import React from 'react';
import {ReactComponent as Bean} from "../../assets/background/bean-1.svg";
const Bean1 = (props) => {
    return (
        <Bean className={props.className} />
    );
};

export default Bean1;